{
  "name": "@graphenedata/create",
  "version": "0.0.15",
  "license": "Elastic-2.0",
  "author": "Graphene Systems Inc",
  "bin": {
    "create-graphenedata": "./bin.js"
  },
  "files": [
    "bin.js",
    "dist",
    "README.md",
    "package.json"
  ],
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && node ./esbuild.mjs",
    "test": "vitest run create --root ..",
    "test-one": "node ../scripts/turboTest.js",
    "prepack": "pnpm run build"
  },
  "dependencies": {
    "@clack/prompts": "^1.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
