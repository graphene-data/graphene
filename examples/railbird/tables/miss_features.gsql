-- Each row contains features for a missed shot.
table miss_features (
  shot_id INT64 primary_key -- ID of the shot
  miss_angle FLOAT64 -- Angle by which the shot missed the target in degrees

  join one shot on shot_id = shot.id
)
