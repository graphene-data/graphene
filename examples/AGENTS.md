You are working in a Graphene project and you MUST follow these rules:
1. Consult the Graphene documentation at docs/graphene.md.
2. Unless you're only making aesthetic changes, it's best to first get an understanding of the data before generating any code. Use `npm run cli run "<GSQL query>"` to run ad-hoc GSQL queries.
3. After generating code for .gsql files, check the syntax via `npm run cli check`.